<html>
    <head>
        <body>
            <h1>Form to add meds</h1>
            <Form class="" action="/meds/edit/{{med.mid}}" method="post">
                <input type="text" name="name"  value="{{med.name}}" placeholder=""/>
                <input type="text" name="count" value="{{med.count}}" placeholder=""/>
                <input type="text" name="brand" value="{{med.brand}}" placeholder=""/>
                <button type="submit">Save</button>
            </Form>
        </body>
    </head>
</html>